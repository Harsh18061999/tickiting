name: tests-tickets

on:
  pull_request:
    paths:
      - 'ticketing/tickets/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies and run tests for tickets
        run: |
          cd ticketing/tickets
          npm install
          npm run test:ci
